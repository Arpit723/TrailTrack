<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>TrailTrack uses your location to track your walking and running route and draw it on the map.</string>
	<key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
	<string>TrailTrack uses your location to track your walking and running route — including while the app is in the background — so your route is recorded without interruption.</string>
	<!-- "location" = Location Updates background mode (NOT Background Fetch). Note: tracking
	     stops if the user force-quits the app; this is expected iOS behavior, not a bug. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>location</string>
	</array>
	<key>UIUserInterfaceStyle</key>
	<string>Light</string>
</dict>
</plist>
